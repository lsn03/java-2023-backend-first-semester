package edu.project4.transformation;

public final class TransformationUtils {
    private TransformationUtils() {

    }

    public static double getSumSquare(double a, double b) {
        return Math.pow(a, 2) + Math.pow(b, 2);
    }

}
